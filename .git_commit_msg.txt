feat: Implement mutation handlers for interviews, offers, and notes

## Changes

### New Mutations Hook (useMutations.ts)
- useCreateInterview: POST /api/interviews with onSuccess invalidation
- useDeleteInterview: DELETE /api/interviews/{id} with confirmation
- useUpdateInterview: PATCH /api/interviews/{id}
- useCreateOffer: POST /api/offers with auto-calculation
- useDeleteOffer: DELETE /api/offers/{id} with confirmation
- useUpdateOffer: PATCH /api/offers/{id}
- useUpdateJobNotes: PATCH /api/jobs/{id} for notes persistence

All mutations include:
✓ React Query integration
✓ Query invalidation on success
✓ Error handling and callbacks
✓ Optimistic update patterns (for UI feedback)

### InterviewsTab Updates
- useCreateInterview and useDeleteInterview integration
- Form validation before submission
- Loading states on submit and delete buttons
- Confirmation dialog before deletion
- Auto-reset form after successful creation
- isPending state for disabled buttons

### OffersTab Updates
- useCreateOffer and useDeleteOffer integration
- Form validation (baseSalary, startDate required)
- Loading states on submit and delete buttons
- Confirmation dialog before deletion
- Auto-reset form after successful creation
- isPending state for disabled buttons

### OverviewTab Updates
- useUpdateJobNotes integration
- Save button toggles edit mode
- Loading state while saving notes
- Auto-sync with job data via useEffect
- Disabled state during save operation
- Server persistence on button click

## Features
✓ Create interviews with type, date, time, interviewer, location
✓ Delete interviews with confirmation
✓ Create offers with salary, bonus, equity, start date
✓ Delete offers with confirmation
✓ Edit and persist job notes
✓ Query invalidation for real-time updates
✓ Proper loading states and disabled buttons
✓ Error handling and user feedback
✓ Confirmation dialogs for destructive actions

## API Integration
- All endpoints support form data and return updated objects
- Mutations handle both success and error cases
- Query client invalidation ensures data consistency

## Status
✅ Interview mutations complete
✅ Offer mutations complete
✅ Notes mutations complete
✅ Loading states implemented
✅ Confirmation dialogs added
✅ Error handling integrated

Next: E2E tests for all mutation flows

Co-Authored-By: Claude Haiku 4.5 <noreply@anthropic.com>
