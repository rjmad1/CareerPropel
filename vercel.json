{
  "crons": [
    {
      "path": "/api/agents/execute-pending",
      "schedule": "0 0 * * *"
    }
  ],
  "buildCommand": "npx prisma migrate resolve --rolled-back 20260523000000_add_jobid_mock_session_unique_fk 2>/dev/null || true; npx prisma migrate deploy && npx prisma generate && next build"
}
